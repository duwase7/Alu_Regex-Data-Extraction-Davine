This project demonstrates a Python program that extracts structured data from raw text using regular expressions (regex).  
It emphasizes **accuracy**, **robustness**, and **security awareness**, handling realistic variations of data.
# Alu_Regex-Data-Extraction-Davine
